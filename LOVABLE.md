# המשחק ב-Lovable

המעבר בוצע ב-17.9.2026. יש עכשיו שני מאגרים, וכדאי להכיר את ההבדל:

| | מאגר | מה יש בו |
|---|---|---|
| **המקורי** | [`pieces-of-us`](https://github.com/p0534191131-glitch/pieces-of-us) (ציבורי) | הגרסה המקומית: Vite + React 18 + Tailwind 3, כולל `server/index.mjs` למשחק ברשת הביתית |
| **של Lovable** | `pieces-of-us-d24cbb43` (פרטי, נוצר על ידי Lovable) | אותו משחק בתבנית של Lovable: TanStack Start + React 19 + Tailwind 4. מסונכרן דו-כיוונית עם הפרויקט |

הפרויקט: <https://lovable.dev/projects/3c5a74b8-608e-4a5a-8662-34a95938e4b3>

> Lovable לא מייבא מאגר קיים. הדרך היחידה היא: לפתוח פרויקט, לחבר אותו לגיטהאב (Lovable פותח מאגר משלו),
> ואז לדחוף את הקוד למאגר ההוא — הוא מסתנכרן לפרויקט תוך שניות.

## מה שונה בגרסה של Lovable

- **נקודת הכניסה**: `src/routes/index.tsx` טוען את `src/App.tsx` בצד הלקוח בלבד (`lazy` + `useEffect`),
  כי כל המשחק נשען על WebSocket, Web Audio, IndexedDB ו-sessionStorage שלא קיימים בצד השרת.
- **RTL**: `src/routes/__root.tsx` מגדיר `<html lang="he" dir="rtl">` ואת הכותרות.
- **עיצוב**: `src/styles.css` טוען את Tailwind 4 ומייבא את מערכת העיצוב הקיימת דרך
  `@config "../tailwind.config.ts"` — כל הטוקנים, האנימציות ו-`tailwindcss-animate` ממשיכים לעבוד בלי שכתוב.
- **בלי שרת מקומי**: `server/` לא מועתק לשם. `/api/photos` ו-`/api/info` פשוט מחזירים 404 והמשחק ממשיך
  עם תמונות הפרימיום ועם תמונות שמעלים מהמחשב.
- **מבנה זהה בשאר הדברים**: `src/game`, `src/net`, `src/screens`, `src/components` הם אותם קבצים בדיוק.

## מה עוד חסר כדי לשחק מכל מקום

להדליק **Lovable Cloud** בפרויקט. ברגע ש-`VITE_SUPABASE_URL` ו-`VITE_SUPABASE_PUBLISHABLE_KEY`
זמינים בצד הלקוח, `createTransport()` עובר לבד ל-`src/net/supabaseTransport.ts` (Broadcast + Presence,
בלי טבלאות ובלי הגדרות נוספות). עד אז המשחק באוויר מציג הודעה שהחיבור בזמן אמת עוד לא הופעל,
ו"אימון לבד" עובד רגיל.

> ⚠️ המימוש של Supabase נכתב לפי ה-API של supabase-js v2 אבל עוד לא נבדק מול פרויקט אמיתי.
> אחרי הדלקת Lovable Cloud צריך לבדוק משחק אמיתי בשני דפדפנים.

## לעבוד על המשחק מכאן והלאה

- **שינוי מקומי** → לדחוף ל-`pieces-of-us`, ואז להעתיק את מה שרלוונטי למאגר של Lovable (או לדחוף לשניהם).
- **שינוי דרך הסוכן של Lovable** → הוא נכנס למאגר של Lovable, וממנו אפשר למשוך חזרה.
- אל תעשו force-push למאגר של Lovable: זה משכתב את ההיסטוריה גם בצד שלהם.
- כל הודעה לסוכן עולה קרדיטים. פתיחת הפרויקט עלתה 0.3 קרדיט; העברת הקוד עצמה — אפס.

## לשים לב

- האתר ב-Lovable ציבורי אחרי פרסום. תמונות אישיות מעלים דרך המשחק, ולא שמים אותן ב-`public/`.
- ב-Supabase Realtime בחינם יש מגבלה של כ-100 הודעות בשנייה לפרויקט. המשחק שולח עד כ-9 עדכוני
  התקדמות בשנייה לכל שחקן — בחדר של 6 זה כבר כ-54 הודעות בשנייה — ומעביר תמונות במנות של 48KB.
