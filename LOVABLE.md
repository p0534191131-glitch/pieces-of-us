# מעבר ל-Lovable

הפרויקט כבר כתוב בשפה של Lovable: Vite + React 18 + TypeScript + Tailwind 3, טוקנים של shadcn כמשתני HSL ב־`src/index.css`, ה-alias `@/`, אייקונים מ־`lucide-react` ורכיבי UI ב־`src/components/ui`. לא צריך "להמיר" — רק לחבר.

## מה משתנה בענן

| מקומי (היום) | ב-Lovable |
|---|---|
| `server/index.mjs` מעביר הודעות בין המחשבים | **Supabase Realtime** (Broadcast + Presence). המימוש כבר כתוב: `src/net/supabaseTransport.ts` |
| עד 6 מחשבים באותה רשת ביתית | משחקים מכל מקום, עד 6 שחקנים בחדר |
| תמונות מתיקיית `photos` | המארח/ת מעלה תמונות מהמחשב; הן עוברות ישירות לשאר השחקנים דרך הערוץ ולא נשמרות בענן |
| פרימיום ב־`public/premium` | עובד כמו שהוא |

ההחלפה אוטומטית: כשמוגדרים `VITE_SUPABASE_URL` ו־`VITE_SUPABASE_PUBLISHABLE_KEY` (Lovable Cloud מגדיר אותם), הפונקציה `createTransport()` טוענת את Supabase במקום השרת המקומי. **לא צריך טבלאות.**

> ⚠️ המימוש של Supabase נכתב לפי ה-API של supabase-js v2, אבל עוד לא נבדק מול פרויקט אמיתי. צריך לבדוק אותו מיד אחרי החיבור.

## שלבים

1. יוצרים פרויקט Lovable (Vite + React) ומפעילים **Lovable Cloud**.
2. מחברים את הפרויקט ל-GitHub ומעתיקים לשם את התוכן של `pieces-of-us/` — בלי `server/`, `node_modules/` ו־`dist/`.
3. שולחים לסוכן של Lovable הודעה אחת מסודרת:

   > זה משחק קיים ומוכן — אל תבנה אותו מחדש. ודא ש-Lovable Cloud פעיל ושהמשתנים VITE_SUPABASE_URL ו-VITE_SUPABASE_PUBLISHABLE_KEY זמינים בצד הלקוח. שמור על המוסכמות הקיימות: הטוקנים ב-src/index.css ו-tailwind.config.ts, רכיבי shadcn, ה-alias @/ ו-lucide-react. אחר כך בדוק בשני חלונות גלישה בסתר: יצירת חדר, הצטרפות עם הקוד, העלאת 3 תמונות והתחלת משחק "ספרינט".

4. בודקים בעצמכם משני מחשבים שונים: חדר ← העלאת תמונות ← ספרינט ← כוחות ← סוף משחק.

## לשים לב

- כל הודעה לסוכן של Lovable עולה קרדיטים, ולכן עדיף הודעה אחת מסודרת.
- האתר ב-Lovable ציבורי. תמונות אישיות מעלים דרך המשחק, ולא שמים אותן בתיקייה `public/`.
- ב-Supabase Realtime בחינם יש מגבלה של כ-100 הודעות בשנייה לפרויקט. המשחק שולח עד כ-9 עדכוני התקדמות בשנייה לכל שחקן — בחדר של 6 זה כבר כ-54 הודעות בשנייה — ומעביר תמונות במנות של 48KB. בחדרים גדולים כדאי לבדוק שאין חריגה.
